/**
 * - `true`: Show dim overlay (but the grid would not response scroll `wheel` event )
 * - `'scroll'`: Show dim overlay and also allow user scrolls
 * - `false`: Don't show dim overlay
 */
export type DimOverlayOptions = boolean | 'scroll';
