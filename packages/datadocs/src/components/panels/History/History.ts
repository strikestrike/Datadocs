import type { PanelComponentConfig } from "../types";

export const historyConfig: PanelComponentConfig = {
  name: "history",
  icon: "history",
  label: "History",
  defaultPlacement: "container:right",
};
