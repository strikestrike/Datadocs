import type { PanelComponentConfig } from "../types";

export const imageConfig: PanelComponentConfig = {
  name: "image",
  icon: "image",
  label: "Image",
  defaultPlacement: "container:right",
};
