import type { PanelComponentConfig } from "../types";

export const infoConfig: PanelComponentConfig = {
  name: "info",
  icon: "info",
  label: "Info",
  defaultPlacement: "container:center:bottom",
};
