import type { PanelComponentConfig } from "../types";

export const presetsConfig: PanelComponentConfig = {
  name: "presets",
  icon: "presets",
  label: "Presets",
  defaultPlacement: "container:right",
};
