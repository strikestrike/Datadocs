import type { PanelComponentConfig } from "../types";

export const queryhistoryConfig: PanelComponentConfig = {
  name: "query-history",
  icon: "query-history",
  label: "Query History",
  defaultPlacement: "container:center:bottom",
};
