import type { PanelComponentConfig } from "../types";

export const shapeConfig: PanelComponentConfig = {
  name: "shape",
  icon: "object-chart",
  label: "Shape",
  defaultPlacement: "container:right",
};
