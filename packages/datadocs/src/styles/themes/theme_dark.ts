import { extend } from "./utils";
import defaultTheme from "./theme_default";

export default extend(defaultTheme, {
  // Sample
  // backgroundPrimary: '#444444',
  // backgroundSecondary: '#7b7b7b',
  // textPrimary: '#fff',
});
